[static]

1. static
	- 정의 : 하나의 클래스로부터 생성된 객체들 중 하나만 생성하는 멤버
	- 특징 : 
		- 클래스를 생성(new)하지 않고 사용할 수 있음
		- 클래스가 객체화되지 않아도 사용할 수 있음
		- 클래스 변수, 클래스 멤버라고도 함
		- this.가 없음(this는 식별자를 뜻함)
		- 클래스가 로드되면 사용 가능	
	- 객체별로 생성하지 않고 공용으로 사용
	
2. 정적 멤버 
	- 종류 : 정적 속성, 정적 메소드, 정적 상수
	